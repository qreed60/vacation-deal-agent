# Deployment environment configuration for vacation-deal-agent.
# Copy this file to .env.deploy and fill in any values as needed.
#   cp .env.deploy.example .env.deploy

# ── SearXNG ──────────────────────────────────────────────
SEARXNG_BASE_URL=http://127.0.0.1:8888
SEARXNG_TIMEOUT_SECONDS=5
SEARXNG_ENABLED=true
SEARXNG_FALLBACK_ENABLED=true
SEARXNG_MAX_RESULTS=10

# ── Amadeus ─────────────────────────────────────────────
AMADEUS_ENABLED=false
AMADEUS_BASE_URL=https://test.api.amadeus.com
AMADEUS_CLIENT_ID=
AMADEUS_CLIENT_SECRET=
AMADEUS_TIMEOUT_SECONDS=8

# ── Google Places ───────────────────────────────────────
GOOGLE_PLACES_ENABLED=false
GOOGLE_PLACES_API_KEY=
GOOGLE_PLACES_TIMEOUT_SECONDS=8

# ── SerpAPI ─────────────────────────────────────────────
SERPAPI_ENABLED=false
SERPAPI_API_KEY=
SERPAPI_BASE_URL=https://serpapi.com/search
SERPAPI_TIMEOUT_SECONDS=8

# ── Fast Flights (free/unofficial) ──────────────────────
FAST_FLIGHTS_ENABLED=false
FAST_FLIGHTS_FETCH_MODE=common
FAST_FLIGHTS_SEAT=economy
FAST_FLIGHTS_MAX_STOPS=
FAST_FLIGHTS_MAX_RESULTS=20

# ── trvl (free/no-key structured source) ────────────────
TRVL_ENABLED=true
TRVL_BINARY_PATH=.tools/trvl/trvl
TRVL_TIMEOUT_SECONDS=120
TRVL_MAX_FLIGHT_RESULTS=20
TRVL_MAX_HOTEL_RESULTS=20
TRVL_CURRENCY=USD
TRVL_ALLOW_RISKY_FLIGHT_OFFERS=false
TRVL_REQUIRE_CONFIGURED_CURRENCY=true

# ── trvl broad discovery (Phase 4C defaults) ───────────
TRVL_BROAD_DISCOVERY_ENABLED=true
TRVL_BROAD_INCLUDE_ONE_WAY_FALLBACKS=true
TRVL_BROAD_ALLOW_RISKY_ALTERNATIVES=true
TRVL_BROAD_MAX_ALTERNATIVES=50

# ── Free-source probe hooks (optional) ──────────────────
FREE_TRAVEL_PROBE_FLIGHTS_SKILL_COMMAND=
FREE_TRAVEL_PROBE_TRAVEL_HACKING_TOOLKIT_COMMAND=

# ── Mock search gating (Phase 4D-3) ────────────────────
# Set to true only for local development/testing.
# Deployed UI must NEVER generate mock results by default.
MOCK_SEARCH_ENABLED=false

# ── AI Search Planner (Phase 5A) ─────────────────────────
# Disabled by default. Enable to use bounded AI planning.
AI_SEARCH_PLANNER_ENABLED=false
AI_SEARCH_PLANNER_PROVIDER=disabled
AI_SEARCH_PLANNER_MODEL=
AI_SEARCH_PLANNER_BASE_URL=
AI_SEARCH_PLANNER_API_KEY=
AI_SEARCH_PLANNER_MAX_STRUCTURED_SEARCHES=8
AI_SEARCH_PLANNER_MAX_RESEARCH_QUERIES=5
AI_SEARCH_PLANNER_ALLOW_DATE_FLEX=false
AI_SEARCH_PLANNER_DATE_FLEX_DAYS=1
AI_SEARCH_PLANNER_TIMEOUT_SECONDS=45

# Phase 5B: Scheduled search configuration
SCHEDULER_ENABLED=true
SCHEDULER_DEFAULT_SEARCHES_PER_DAY=2
SCHEDULER_DEFAULT_JITTER_MINUTES=20
SCHEDULER_LOCK_DIR=data/locks
SCHEDULER_LOCK_TTL_SECONDS=7200
SCHEDULER_TIMER_INTERVAL_MINUTES=60
